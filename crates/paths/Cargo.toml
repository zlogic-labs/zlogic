[package]
name = "zlogic-paths"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Path helpers for the whole Zlogic system: normalise / physical_resolve / verbatim stripping. std-only, shared by zlogic-policy and zlogic-store."
license.workspace = true

[lints]
workspace = true

[lib]
name = "zlogic_paths"
path = "src/lib.rs"
